<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="mapper.StudioMapper">
    <select id="showinfo" parameterType="String" resultType="Studio">
        select * from studio where pnumber = #{pnumber}
    </select>
    <select id="getNI" resultType="Studio">
        select * from studio
    </select>
</mapper>
